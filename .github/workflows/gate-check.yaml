name: CaskMCP Gate

on:
  pull_request:
    branches: [main]
    paths:
      - 'caskmcp/core/**'
      - 'caskmcp/mcp/**'
      - 'caskmcp/cli/**'
      - 'dogfood/github/**'
  workflow_dispatch:

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run CaskMCP gate
        uses: ./.github/actions/caskmcp-gate
        with:
          toolpack: dogfood/github/toolpack.yaml
          lockfile: dogfood/github/lockfile/caskmcp.lock.yaml
          baseline: dogfood/github/artifact/baseline.json
