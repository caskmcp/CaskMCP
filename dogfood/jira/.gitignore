# Full spec cache (large ~2.5MB JSON, downloaded on demand)
.full-spec-cache.json

# Local CaskMCP state (approval snapshots, keys)
.caskmcp/

# macOS metadata
.DS_Store

# Drift check artifacts (generated, not committed)
spec-drift.diff
spec-drift-summary.json
